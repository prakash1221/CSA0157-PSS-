#include<stdio.h>
int main()
{
	int n,i,fact=1;
	printf("enter the factorial: ");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		fact*=i;
		printf("%d *",i);
	}
	printf("\nfactorial of %d = %d",n,fact);
	
	return 0;
}
